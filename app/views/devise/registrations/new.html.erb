<link href='http://fonts.googleapis.com/css?family=Indie+Flower' rel='stylesheet' type='text/css'>
<link rel="stylesheet" type="text/css" href="/addnewzone.css">

<div class="magicSentence">Sign up</h2>
<center>
    <div class="loginBox">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>
        <div class="input"><%= f.label :username%><br />
        <%= f.text_field :username, autofocus: true, :class=>"input" %></div>
        <div class="input"><%= f.label :email %><br />
        <%= f.email_field :email, :class=>"input" %></div>

        <div class="input"><%= f.label :password %> <% if @validatable %><i>(<%= @minimum_password_length %> characters minimum)</i><% end %><br />
          <%= f.password_field :password, autocomplete: "off", :class=>"input" %></div>

        <div class="input"><%= f.label :password_confirmation, :class=>"input" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off", :class=>"input" %></div>

        <div><%= f.submit "Sign up", :class=>"submit" %></div>
      <% end %>
    <%= render "devise/shared/links" %>
    </div>
</center>